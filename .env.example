# Example environment variables for OmniContext
# Copy this file to .env and adjust as needed

# Override the default data directory (default: %LOCALAPPDATA%\OmniContext)
# OMNICONTEXT_DATA_DIR=D:\MyData\OmniContext

# Start the application with capturing paused on first run
# OMNICONTEXT_START_PAUSED=1
